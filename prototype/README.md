1) download homebrew using /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    -     echo >> /Users/xiao/.zprofile
    echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> /Users/xiao/.zprofile
    eval "$(/opt/homebrew/bin/brew shellenv)"

    - run that 
2) using homebrew to install Azure CLI on macOS
    - brew update && brew install azure-cli
    - az --version to check if uploaded

3) create prototype structure
4) create virtual environment
6) mkdir prototype
7) cd prototype

5) pip3 install -r /Users/xiao/Documents/Python/504assignment_final/prototype/requirements.txt


